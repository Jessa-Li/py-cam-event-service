# Python virtual environments
.venv/
venv/

# Python bytecode and caches
__pycache__/
*.py[cod]
*$py.class

# Test / build caches
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Local environment / secrets
.env
.env.*
!.env.example

# macOS
.DS_Store

# Editor / IDE
.idea/
.vscode/
*.swp

# Playwright MCP scratch + screenshot artifacts from local debugging.
# These live at the repo root only; the frontend has its own gitignore
# for node_modules / dist.
.playwright-mcp/
/*.png

# Local-only Claude Code MCP server config; per-machine tool registration.
.mcp.json
